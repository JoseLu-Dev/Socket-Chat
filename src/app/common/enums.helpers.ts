/**
 * Enum to handle Socket Events names
 */
export enum SocketsEvents {
    connection = 'connection',
    writing = 'writing',
    message = 'msg',
    identification = 'identification',
}
