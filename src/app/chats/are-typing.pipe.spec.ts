import { AreTypingPipe } from './are-typing.pipe';

describe('AreTypingPipe', () => {
  it('create an instance', () => {
    const pipe = new AreTypingPipe();
    expect(pipe).toBeTruthy();
  });
});
